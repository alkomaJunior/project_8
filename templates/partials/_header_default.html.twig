{% block body %}
        {% block header_img %}
            <div class="container-header">
                <img class="slide-image" src="{{ asset('build/images/todolist_content.jpg') }}" alt="todo list">
                <div class="centered"><h1>{{ header_title }}</h1>
                    {% include 'partials/_flashes.html.twig' %}</div>
            </div>
        {% endblock %}
{% endblock %}

